import os

async def get_outfit_recommendations(wardrobe_items: list, body_type: str) -> str:
    # Mocking gemini to bypass dependency issues for the demo
    return "This is a mocked outfit recommendation. Due to a dependency issue, the LLM integration is disabled for the demo."
